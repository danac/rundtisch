[workspace]
resolver = "2"
members = [".", "worker"]
default-members = ["."]

[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[features]
default = []
native = ["dep:tokio", "axum/tokio", "dep:sqlx"]
cloudflare = ["dep:worker"]

# Shared — safe for both runtimes
[dependencies]
axum = { version = "0.8", default-features = false, features = ["json", "http1"] }
#serde = { version = "1.0", features = ["derive"] }
#thiserror = "2.0.18"
serde_json = "1.0"
email_address = "0.2.9"
sea-query = "1.0.1"
sqlx = { version = "0.9.0", features = ["runtime-tokio", "sqlite"], optional = true}
time = { version = "0.3.53", default-features = false, features = ["formatting", "parsing"] }
tokio = { version = "1.52", features = ["macros", "rt-multi-thread", "net", "signal"], optional = true }
worker = { version = "0.8", features = ["http", "axum"], optional = true }

[[bin]]
name = "native_server"
path = "src/bin/native_server.rs"
required-features = ["native"]
