{
  "name": "rundtisch",
  "private": true,
  "scripts": {
    "dev": "concurrently -n fe,api -c blue,green \"npm run dev --prefix web\" \"DATABASE_URL=sqlite://rundtisch.sqlite?mode=rwc cargo run --manifest-path ../Cargo.toml -p rundtisch-demo --bin migrate && DATABASE_URL=sqlite://rundtisch.sqlite?mode=rwc cargo run --manifest-path ../Cargo.toml -p rundtisch-demo --bin native\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
