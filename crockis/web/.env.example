# When false, the SPA calls the REST API instead of in-memory placeholders.
VITE_USE_MOCK=true

# Origin for /collections, /collections/:id/photos, and /auth/*.
# Leave as /api to use the Vite proxy (same pattern as the rundtisch demo).
VITE_API_BASE=/api
